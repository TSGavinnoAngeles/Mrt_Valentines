import React from 'react';

interface StationProps {
  data: { label: string };
  position: { x: number; y: number };
} 

const Station: React.FC<StationProps> = ({ data, position }) => {
  return (
    <div>
      

    </div>
  );
};

export default Station;
